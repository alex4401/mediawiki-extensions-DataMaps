<?php
namespace Ark\DataMaps\Rendering;

use Parser;
use PPFrame;

final class ParserFunction_EmbedDataMap {
    public function run( Parser $parser, PPFrame $frame, array $args ): string {
        //new DataMapEmbedRenderer($title, $this->getData()->getValue(), $parser);
		//$embed = $this->getEmbedRenderer( $parser );
		//$embed->prepareOutputPage();
		//$output->setText( $output->getRawText() . $embed->getHtml() );
    }
}